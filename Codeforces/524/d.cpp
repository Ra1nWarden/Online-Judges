#include <cstdio>

using namespace std;

int main() {
    int tc;
    scanf("%d", &tc);
    while(tc--) {
        int n;
        long long k;
        scanf("%d %lld", &n, &k);
        
    }
    return 0;
}